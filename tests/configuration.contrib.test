<?php
/**
 * @file
 * Test cases for contrib modules supported by configuration.
 */

/**
 * Base class for configuration contrib test cases.
 */
class ConfigurationContribWebTestCase extends ConfigurationWebTestCase {
  /**
   * Set up test.
   */
  public function setUp($modules = array()) {
    $modules[] = 'configuration';
    $modules[] = 'configuration_test';
    parent::setUp($modules);

    $this->adminLogin();
  }
}
